// import Swiper JS
// core version + navigation, pagination modules:
const reviewsSlider = new Swiper('.phone_slider', {
    loopedSlides: 5,
    loop: true,
    slidesPerView: "auto",
    centeredSlides: true,
});